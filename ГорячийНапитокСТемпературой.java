//  Наследник класса ГорячийНапиток с дополнительным полем "температура"
public class ГорячийНапитокСТемпературой extends ГорячийНапиток {
    private final int температура;  // Температура напитка

    //  Конструктор: вызываем родительский + добавляем температуру
    public ГорячийНапитокСТемпературой(String name, int volume, int температура) {
        super(name, volume);  // 🔹 Вызываем конструктор родителя
        this.температура = температура;
    }

    //  Геттер для температуры
    public int getТемпература() {
        return температура;
    }

    //  Переопределение метода для вывода информации
    @Override
    public String toString() {
        return super.toString() + ", температура: " + температура + "°C";
    }
}
